{
  "name": "cloudinit-assistant",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that grounds Gemini answers on cloudinit.online content",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
